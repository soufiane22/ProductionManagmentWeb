
export class MonthYear{
    id:number;
    date:string;
    monthYear:string

     constructor(id?: number, date?: string, monthYear?: string) {
    this.id = id
    this.date = date
    this.monthYear = monthYear
  }
}