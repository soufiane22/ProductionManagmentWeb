import { CalloutDirectiveDirective } from './callout-directive.directive';

describe('CalloutDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CalloutDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
